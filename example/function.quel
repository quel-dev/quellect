type vector = cons::<int, int>;
type two_vector = cons_two::<vector, vector>;

tmp = (cons 1, 2);

tmp2 = (cons_two tmp, (cons 3, 4));

function add(cons_two::<a, cons::<x, t> >, cons::<n, m>) {
  c = x + y;
}

(add tmp2, tmp);
